import pytest
from ioet_exercise.src.controllers import coincidenceController


def test_compare_day_hours(self, employee1, employee2, coincidences):
    pytest.assume(value == created_beverage[param])
    pytest.assume(created_beverage["_id"])
